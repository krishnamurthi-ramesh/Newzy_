# SmartScrapAI Environment Variables Example
# Copy this file to .env and fill in your own values

# API Keys (Required)
DEVTO_API_KEY=your_devto_api_key_here

# Application Settings
DEBUG=True
SECRET_KEY=change_this_to_a_random_secret_key

# Caching Configuration
CACHE_TYPE=simple
CACHE_DEFAULT_TIMEOUT=300
CACHE_THRESHOLD=500

# Model Settings
DEFAULT_MODEL=flux
IMAGE_WIDTH=800
IMAGE_HEIGHT=400

# Optional: Hashnode API Credentials (if using Hashnode)
# HASHNODE_API_TOKEN=your_hashnode_token
# HASHNODE_PUBLICATION_ID=your_publication_id

# Optional: Replicate API Token (if using Replicate)
# REPLICATE_API_TOKEN=your_replicate_token

# Note: Do not commit your actual .env file to version control
# This example file shows the required format but doesn't contain real credentials 